export class InfoClass
{
    fullName:string;
    basicSalary:number;
    address:string;

    constructor(fullName:string, basicSalary:number, address:string)
    {
       this.fullName = fullName;
       this.basicSalary = basicSalary;
       this.address = address;
    }

}